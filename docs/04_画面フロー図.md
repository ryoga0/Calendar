# 病院予約カレンダーアプリ 画面フロー図

| 文書ID | HOSP-CAL-SF-001 |
|--------|-----------------|
| 版数 | 2.0 |
| 作成日 | 2026-04-07 |
| 最終更新 | 2026-04-09 |
| 参照 | 01_要件定義書.md、02_基本設計書.md |

---

## 1. 概要

本書は、現在実装されている患者向け画面の遷移を整理したものとする。  
管理者画面は現フェーズでは未実装のため、本書では対象外とする。

---

## 2. 全体サイトマップ

```mermaid
flowchart TB
  P0[SC-01 公開トップ]
  P1[SC-02 ログイン]
  P2[SC-03 新規登録]
  P3[SC-04 ホーム]
  P4[SC-05 新しい予約]
  P5[SC-06 予約一覧]
  P6[SC-07 予約詳細]
  P7[SC-08 予約変更]
  P8[SC-09 プロフィール]

  P0 --> P1
  P0 --> P2
  P1 --> P3
  P2 --> P3
  P3 --> P4
  P3 --> P5
  P3 --> P8
  P5 --> P6
  P6 --> P7
  P6 --> P5
  P7 --> P6
  P8 --> P3
```

---

## 3. 公開トップからログインまで

```mermaid
flowchart TD
  A[アプリ起動] --> B{ログイン済み?}
  B -->|Yes| C[SC-04 ホーム]
  B -->|No| D[SC-01 公開トップ]
  D --> E[SC-02 ログイン]
  D --> F[SC-03 新規登録]
  E --> G{認証成功?}
  G -->|Yes| C
  G -->|No| E
  F --> H{登録成功?}
  H -->|Yes| C
  H -->|No| F
```

---

## 4. ホーム画面

ホームでは次の導線を持つ。

- 次の予約を見る
- 診療科から新しい予約を取る
- 予約一覧を見る
- プロフィールを見る
- ログアウトする

```mermaid
flowchart TD
  H[SC-04 ホーム] --> B1[診療科を選ぶ]
  H --> B2[予約一覧を見る]
  H --> B3[プロフィール]
  H --> B4[ログアウト]
  B1 --> N[SC-05 新しい予約]
  B2 --> L[SC-06 予約一覧]
  B3 --> P[SC-09 プロフィール]
  B4 --> T[SC-01 公開トップ]
```

---

## 5. 新しい予約フロー

現在の実装では、確認ページを分けず、時間ボタンの押下で予約を確定する。

```mermaid
flowchart TD
  A[SC-04 ホーム] --> B[診療科を選ぶ]
  B --> C[SC-05 新しい予約]
  C --> D[日付を選ぶ]
  D --> E[空き時間を取得]
  E --> F{候補あり?}
  F -->|No| G[空きなしメッセージ]
  G --> D
  F -->|Yes| H[時間ボタンを押す]
  H --> I{登録成功?}
  I -->|Yes| J[SC-06 予約一覧]
  I -->|No| K[理由を表示]
  K --> C
```

---

## 6. 予約一覧フロー

```mermaid
flowchart TD
  A[SC-04 ホーム] --> B[SC-06 予約一覧]
  B --> C{予約あり?}
  C -->|No| D[空状態メッセージ]
  D --> E[ホームへ戻る]
  C -->|Yes| F[予約カードを選択]
  F --> G[SC-07 予約詳細]
```

---

## 7. 予約詳細フロー

```mermaid
flowchart TD
  A[SC-07 予約詳細] --> B[日時を変更する]
  A --> C[キャンセルする]
  A --> D[一覧へ戻る]
  B --> E[SC-08 予約変更]
  C --> F{確認ダイアログ}
  F -->|いいえ| A
  F -->|はい| G{削除成功?}
  G -->|Yes| H[SC-06 予約一覧]
  G -->|No| I[エラー表示]
  I --> A
  D --> H
```

---

## 8. 予約変更フロー

```mermaid
flowchart TD
  A[SC-08 予約変更] --> B[日付を選ぶ]
  B --> C[空き時間を取得]
  C --> D{候補あり?}
  D -->|No| E[空きなしメッセージ]
  E --> B
  D -->|Yes| F[時間ボタンを押す]
  F --> G{更新成功?}
  G -->|Yes| H[SC-07 予約詳細]
  G -->|No| I[理由を表示]
  I --> A
```

---

## 9. プロフィール更新フロー

```mermaid
flowchart TD
  A[SC-09 プロフィール] --> B[名前と電話番号を編集]
  B --> C[保存]
  C --> D{更新成功?}
  D -->|Yes| E[成功メッセージ表示]
  D -->|No| F[エラーメッセージ表示]
  E --> A
  F --> A
```

---

## 10. 戻るボタンの挙動

現在の実装では、共通レイアウト `PageShell` に戻るボタンを持たせている。

戻る先の優先順位:

1. ページごとに `backTo` が指定されている場合はそのページへ戻る
2. ブラウザ履歴がある場合は 1 つ前に戻る
3. 履歴がない場合は `/` に戻る

---

## 11. 画面 ID 一覧

| ID | 画面名 |
|----|--------|
| SC-01 | 公開トップ |
| SC-02 | ログイン |
| SC-03 | 新規登録 |
| SC-04 | ホーム |
| SC-05 | 新しい予約 |
| SC-06 | 予約一覧 |
| SC-07 | 予約詳細 |
| SC-08 | 予約変更 |
| SC-09 | プロフィール |

---

## 改訂履歴

| 版数 | 日付 | 変更内容 |
|------|------|----------|
| 1.0 | 2026-04-07 | 初版作成 |
| 2.0 | 2026-04-09 | 現在の患者向け画面構成に合わせて全面更新。slot 前提の予約確認フローや未実装管理画面中心の記述を整理 |
